name: Deploy ZOFIA to Ethereum

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install OpenZeppelin via npm
        run: npm install @openzeppelin/contracts

      - name: Create src directory and copy contract
        run: |
          mkdir -p src
          cp contracts/ZOFIA.sol src/

      - name: Create remappings.txt
        run: echo "@openzeppelin/contracts/=node_modules/@openzeppelin/contracts/" > remappings.txt

      - name: Deploy to Ethereum
        env:
          PRIVATE_KEY: ${{ secrets.PRIVATE_KEY }}
        run: |
          forge create --rpc-url https://ethereum.publicnode.com \
            --private-key $PRIVATE_KEY \
            src/ZOFIA.sol:ZOFIA \
            --constructor-args $(cast wallet address --private-key $PRIVATE_KEY) \
            --gas-price 1gwei
